# 技术.md — 项目2 核心设计分析（骨架篇）

> 本文分析当前骨架（`app/config.py`、`app/main.py`）里的三个核心设计：**Pydantic Settings、lifespan、统一异常处理器**。
> 每个设计按「是什么 → 为什么这么做 → 关键细节/坑 → 面试怎么答 → 还能怎么改进」五段展开。
> 这三块同时对应 HELLO 面试复盘里的待优化点，面试可讲"我从上个项目复盘后，在这里做了对"。

---

## 1. Pydantic Settings 配置管理（`app/config.py`）

### 1.1 是什么

用 `pydantic-settings` 的 `BaseSettings` 把所有环境变量集中管理，字段名大小写不敏感地匹配环境变量，并自动做类型转换。

```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
        protected_namespaces=(),
    )

    model_a_name: str = "deepseek-chat"
    model_a_base_url: str = ""
    model_a_api_key: str = ""
    model_b_name: str = "deepseek-chat"   # ← 物理隔离：独立字段
    ...
    top_k: int = 5                         # 环境变量 TOP_K="5" 会被转成 int
    qdrant_path: str = "./qdrant_data"

settings = Settings()                      # 模块级单例
```

### 1.2 为什么这么做

- **配置与代码分离**：改模型、改地址、改参数只需改 `.env`，不动代码。
- **密钥不进代码库**：`.env` 进 `.gitignore`，`API_KEY` 不硬编码。
- **类型安全**：Pydantic 自动把字符串环境变量转成声明类型（`top_k` 变 int、`llm_timeout` 变 float），非法值直接报错，比手写 `os.getenv` + `int()` 安全。
- **集中管理**：所有配置一个类，一眼看清系统有多少可调参数。

### 1.3 关键细节 / 坑（面试最能体现深度的地方）

1. **`pydantic-settings` ≠ `pydantic`**：`BaseSettings` 在 `pydantic-settings` 包里，不是核心 `pydantic`。很多面试者分不清，装错包会 `ImportError`。

2. **`protected_namespaces=()` 是必须的**（HELLO 复盘里专门记过）：
   Pydantic v2 默认保留 `model_` 命名空间（因为 `model_dump`、`model_validate` 等方法都叫 `model_xxx`）。所以字段名 `model_a_name` 会撞这个保留前缀，触发 `UserWarning` 甚至报错。设置空元组 `()` 关闭保护。
   > 面试答法："Pydantic v2 有个保护命名空间的机制，`model_` 开头的字段会冲突，我显式关闭了它，否则 `model_a_name` 这种字段根本没法用。"

3. **`extra="ignore"`**：忽略 `.env` 里未声明的变量，不会因为多写了一个变量就报错。

4. **`env_file_encoding="utf-8"`**：Windows 上 `.env` 默认可能按 GBK 读，中文注释会乱码甚至报错，显式指定 UTF-8。

5. **字段名匹配规则**：`model_a_name` 会自动匹配环境变量 `MODEL_A_NAME`（大小写不敏感、忽略下划线差异）。所以 `.env` 里写 `MODEL_A_NAME`，类里写 `model_a_name`，自动对上。

### 1.4 面试怎么答

- **Q：为什么用 pydantic-settings 而不是 os.getenv？**
  A：`os.getenv` 拿回来全是字符串，还要手动 `int()`、`bool()` 转换，错了没有报错提示；pydantic-settings 声明类型自动转换 + 校验 + 默认值 + 从 `.env` 加载，一套搞定。

- **Q：换模型后端要不要改代码？**
  A：不用。模型A/B 是分离的独立字段，换 DeepSeek→Ollama 只改 `.env` 的 `MODEL_A_BASE_URL` 等，代码零改动。这就是"配置与代码分离"的落地。

- **Q：为什么模型A/B 分成两套字段？**
  A：这是双模型**物理隔离**——生成模型和评审模型用不同的 `model_b_*` 配置，可以指向不同厂商/本地模型，保证"生成 + 评审"的独立性，而不是自己写自己评。（这正是 HELLO 的待优化点，这里从设计之初就做对了。）

### 1.5 还能怎么改进

- 敏感字段可用 `SecretStr` 替代 `str`，打印对象时自动脱敏（`api_key` 不被完整打印进日志）。
- 生产可加多环境配置（dev/prod 不同 `.env`），或接入配置中心（Nacos/etcd）。

### 1.6 `.env` 与 `config.py` 的关系（易混点：值 vs 结构）

**先纠正一个常见误解**：`.env` 不是代码、也不是「全局定义」，它只是一个纯文本文件，每行一个 `KEY=VALUE` 字符串键值对，**没有类型、没有默认值、没有校验**（`true` 只是字符串 `"true"`，不是 Python 的 `True`）。

两者不是「两种配置写法」，而是同一条管道的前后两段：

```
.env 文件（值）  ──读取──▶  config.py 的 BaseSettings（结构/解析/校验）  ──▶  强类型 settings 对象
  纯文本 key=value               字段名 + 类型 + 默认值 + 校验             settings.use_dense_retrieval == True
```

| | `.env` | `config.py`（Pydantic Settings） |
|---|---|---|
| 是什么 | 数据的**载体** | 配置的**契约/结构**（schema + 解析器） |
| 存什么 | **值**：密钥、地址、开关 | **字段名、类型、默认值、校验规则** |
| 有没有类型 | 无，全是字符串 | 有（`bool` / `int` / `float`） |
| 进不进版本库 | 不进（`.gitignore`） | 进（随代码走） |

**为什么拆开**：值会变、要保密 → 放 `.env`；结构是「程序契约」、要类型安全 → 放代码。一个例子：`.env` 写 `USE_DENSE_RETRIEVAL=true` → Pydantic 把字符串 `"true"` 转成 Python `bool`，代码里才能写 `if settings.use_dense_retrieval:`；手写 `os.getenv(...) == "true"` 则要自己处理 `"True" / "1" / "true"` 各种形态。

**两个易混细节**：
1. **字段名大小写不敏感**：`.env` 写 `MODEL_A_API_KEY`，类里写 `model_a_api_key`，自动对上（忽略大小写与下划线差异），不用自己保证逐字符一致。
2. **环境变量优先级**：真实系统环境变量 > `.env` 文件。本地用 `.env`、生产用真实环境变量注入密钥，正是靠这个。

---

## 2. lifespan 生命周期（`app/main.py`）

### 2.1 是什么

用 `asynccontextmanager` 把应用的启动/关闭逻辑写进一个异步上下文管理器，传给 `FastAPI(lifespan=...)`。

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    print("🚀 启动中 ...")   # yield 之前 = startup
    yield
    print("👋 关闭")         # yield 之后 = shutdown
```

### 2.2 为什么这么做（核心：弃用 `@app.on_event`）

FastAPI 早期用 `@app.on_event("startup")` / `@app.on_event("shutdown")` 装饰器，但**官方已弃用**（会触发 DeprecationWarning），推荐 `lifespan`。理由：

1. **一个函数管两件事**：startup 和 shutdown 逻辑在同一个 `lifespan` 里，`yield` 前后分界，读起来更清晰，而不是散落在两个装饰器函数里。
2. **资源清理有保证**：`asynccontextmanager` 的 `yield` 之后代码**即使启动逻辑抛异常也会执行**（`finally` 语义），保证连接/模型被释放。
3. **更符合 Python 异步生态**：`asynccontextmanager` 是标准库原生模式。

> 这是 HELLO 复盘的待优化点：HELLO 的 `main.py` 用了废弃的 `@app.on_event`，这里改成 lifespan。

### 2.3 关键细节

- **`yield` 是分界线**：之前 = 启动（预热 embedding 模型、建 Qdrant 连接、加载索引），之后 = 关闭（释放连接、保存状态）。
- **应用工厂模式 `create_app()`**：返回 app 实例，而不是模块级直接 `app = FastAPI()`。好处是**测试时每个用例用独立 app**、可传不同配置。
- 本项目后续会在 lifespan 里做"预热 embedding 模型"（否则第一次请求要冷加载 bge 模型 ~13 秒，这是 HELLO 实测踩过的坑）。

### 2.4 面试怎么答

- **Q：`@app.on_event` 和 `lifespan` 区别？**
  A：`@app.on_event` 是废弃 API，startup/shutdown 拆成两个独立装饰器；`lifespan` 用异步上下文管理器把两者合并，`yield` 前后分界，且清理逻辑有 `finally` 保证。官方推荐 lifespan。

- **Q：lifespan 里一般做什么？**
  A：启动时加载重资源——比如预热 embedding 模型、建立数据库/向量库连接池、加载索引；关闭时释放连接、落盘状态。这些"重且只做一次"的事放 lifespan，而不是每个请求都做。

- **Q：为什么要应用工厂 `create_app()`？**
  A：便于测试（每个测试拿独立 app 实例、不同配置）、多环境部署（同一套代码生成不同配置的 app）。

### 2.5 还能怎么改进

- 把 lifespan 里的"预热/连接"逻辑抽到单独函数，避免 `main.py` 臃肿。
- 加启动探活：启动时检查 Qdrant/LLM 是否可用，不可用则降级（沿用 HELLO 的"启动时探活降级"哲学）。

---

## 3. 统一异常处理器（`app/main.py`）

### 3.1 是什么

注册一个捕获所有未处理异常的 handler，返回结构化 JSON，而不是 FastAPI 默认的裸 500 / HTML 错误页。

```python
@app.exception_handler(Exception)
async def unhandled_exception_handler(request: Request, exc: Exception):
    return JSONResponse(
        status_code=500,
        content={"error": "internal_error", "detail": str(exc)},
    )
```

### 3.2 为什么这么做

- **统一的错误契约**：客户端（前端/调用方）拿到的永远是 `{"error": ..., "detail": ...}` 这种结构，好解析，不用判断"这次是 JSON 还是 HTML"。
- **对比 HELLO**：HELLO 的 `routes.py` 是在每个接口里 `try/except` 手动抛 500（面试复盘 §10 明确写了这个可改进点）。统一异常处理器把错误处理集中到一处，路由代码更干净、行为一致。

### 3.3 关键细节 / 坑

1. **`str(exc)` 直接暴露给用户有安全风险**（⚠️ 当前骨架的隐患，面试可主动讲）：
   `str(exc)` 可能包含内部路径、SQL、甚至敏感信息。生产上应**对外返回笼统的 `"internal_error"`，把完整堆栈 `logger.exception` 记到服务端日志**，而不是返回给客户端。

2. **捕获 `Exception` 的范围**：注册在 `Exception` 上会兜住所有未处理异常，包括一些本应让框架处理的。更好的做法是**区分异常类型**：
   - 业务异常（自己抛的 `AppException`）→ 返回对应 4xx 语义。
   - 系统异常（未预期）→ 返回 5xx + 记录日志。

3. **可扩展**：可以针对特定异常注册专门 handler，例如 `RequestValidationError`（参数校验失败）返回 422 + 字段级错误说明，比默认更友好。

### 3.4 面试怎么答

- **Q：为什么要统一异常处理器？**
  A：保证所有接口的错误返回格式一致，客户端好处理；把错误处理从业务路由里抽离，路由只写 happy path。

- **Q：直接把 `str(exc)` 返回给用户有什么问题？**
  A：可能泄露内部路径/敏感信息。正确做法是**对外返回通用错误码，对内记录完整堆栈**——区分"给用户看什么"和"给自己看什么"。

- **Q：业务异常和系统异常怎么处理？**
  A：定义业务异常基类 `AppException`（带错误码 + 用户可读 message），统一 handler 里判断类型——业务异常返回 4xx + 明确 message，系统异常返回 5xx + 通用提示 + 记录日志。

### 3.5 还能怎么改进

- 建立业务异常体系：`AppException` 基类 + `NotFoundError` / `AuthError` / `LLMTimeoutError` 等子类，handler 里按类型分发。
- 配合 `request_id`：异常响应里带上 request_id，日志里记录同一 id，做到"一个请求的报错能在日志里定位"（这是 HELLO 复盘 §12.3 的待优化点，项目后续会做）。

---

## 4. 三者串起来（面试一分钟串讲）

> "骨架层我做了三个设计，都是复盘上个项目后做对的：
> ① 配置用 **pydantic-settings** 集中管理，模型A/B 字段分离实现**物理隔离**，注意关了 `protected_namespaces` 才能用 `model_a_name` 这种字段；
> ② 生命周期用 **lifespan** 替代废弃的 `@app.on_event`，后面会在里面预热 embedding 模型；
> ③ **统一异常处理器** 保证错误返回格式一致，业务异常和系统异常分开处理，且对外不泄露内部信息。
> 这三个都属于'工程正确性'，面试官看到的是我从'能跑'进化到'能上线'的意识。"

---

## 5. 待补充（后续阶段的分析）

- D2：LangGraph 的 State / Node / checkpointer 设计分析。
- D5 ✅（见第 8 节）：记忆分层 —— 工作记忆 vs 长期记忆 + `importance`/`source`。
- D6-D7：episodic TTL/importance 排序 + 固化（去重/矛盾合并）分析。
- D8：多智能体编排 + ContextVar 协程隔离分析。
- D10：Function Calling 工具 + 熔断器 + 防注入分析。

---

## 6. B：Checkpointer 稳健化 + 实测抓到的 3 个坑（2026-09-01）

> 本次在把框架"真跑起来"时，实测揪出并在运行时修掉了 **3 个坑**。每个都是极好的面试素材，
> 建议背下来 —— 它们证明你不是"只写了能跑通的代码"，而是"踩过真坑、能定位根因"。

### 坑 1：`SqliteSaver` 是同步版，不支持异步（最隐蔽）

**表象**：起了服务，`/api/v1/ask` 报 `The SqliteSaver does not support async methods.
Consider using AsyncSqliteSaver instead`。

**根因**：LangGraph 的 checkpoint 分两套实现：
- `SqliteSaver` —— 同步，只支持 `invoke/get_state` 等同步调用；
- `AsyncSqliteSaver` —— 异步，支持 `ainvoke/astream`。

我们的应用是 FastAPI 异步，用 `graph.ainvoke`/`.astream`，**必须配 `AsyncSqliteSaver`**，否则上面的报错。

**为什么容易踩**：初学者只看 LangGraph 文档的 checkpointer 示例，很多写的是 `SqliteSaver`（同步演示），
一搬到异步 web 应用里就炸。只有真跑起来才会发现 —— 这正是"看视频会、上手就错"的典型反差。

**修复**：
```python
import aiosqlite
from langgraph.checkpoint.sqlite.aio import AsyncSqliteSaver

conn = await aiosqlite.connect(settings.sqlite_memory_path)
return AsyncSqliteSaver(conn)   # 需要 pip install aiosqlite
```

### 坑 2：`from_conn_string` 返回的是上下文管理器，不能直接当 checkpointer

**表象**（若不提前发现，会与坑 1 连锁）：`AsyncSqliteSaver.from_conn_string(path)` 返回的是
**上下文管理器**，需要 `async with` 进入后才是 saver；直接当 `checkpointer=` 传会报错。

**修复**：不做 `from_conn_string`，改为**手动 `aiosqlite.connect` 再 `AsyncSqliteSaver(conn)`**。
连接对象由我们自己持有（`_checkpointer_conn`），shutdown 时 `await conn.close()`，避免资源泄漏。

> 面试答法："我不用 `from_conn_string` 这种一行流 API，因为它在异步下是个上下文管理器，生命周期不好控制；
> 我自己建连接传给 saver，连接归我管，能显式关闭 —— 这是对资源生命周期的控制意识。"

### 坑 3：Qdrant 本地模式"一个文件夹只能一个客户端"

**表象**：日志里 `Storage folder ./qdrant_data is already accessed by another instance of
Qdrant client. If you require concurrent access, use Qdrant server instead.`

**根因**：RAG 的 `DenseRetriever` 和 episodic 记忆**各自 new 了一个本地 Qdrant 客户端**，都指向
`./qdrant_data`。本地嵌入式模式（`QdrantClient(path=...)`）**一个文件夹只能被一个客户端实例打开**，
两个客户端就冲突。

**修复**：建一个**全局唯一客户端**（`app/core/qdrant.py` 的 `get_qdrant_client()` 单例），
`DenseRetriever` 与 `EpisodicMemory` 都复用它。不同 collection（knowledge_base / agent_memory_episodic）
落在同一个持久化文件夹里，互不冲突，也省一个连接开销。

> 面试答法："本地嵌入式 Qdrant 有单客户端限制，我用了共享客户端单例，把 RAG 和记忆的统一到一处，
> 既避免并发冲突，也避免重复建连接。"

### 坑 4（顺带）：async 节点不能用同步 lambda 包裹

**表象**：`Expected dict, got <coroutine object memory_node ...>`。
**根因**：`lambda s: memory_node(services, s)` 是**同步函数返回协程**，LangGraph 不会 await 它。
**修复**：用 `functools.partial(memory_node, services)` —— partial 保留了 awaitable 属性，LangGraph 才能 await。

### 坑 5（9/2 LLM 打通时抓到）：reviewer 格式化评分报了 NameError

**表象**：`.env` 填好 key 后，`/ask` 的 `review_comment` 返回 `模型B 评审失败（NameError），返回模型A 原始回答`，`review_score=null`。
**根因**：`reviewer.py` 的 `_format_comment` 里，列表推导式循环变量是 `k`，但 f-string 却写了 `{v}`：
```python
dims = [f"{k}={v}" for k in ("accuracy", "completeness", "safety", "relevance") ...]  # v 未定义
```
**为什么之前没暴露**：`_format_comment` 只在「模型B 成功返回并解析出评分」之后才执行。昨天没 key，模型B 根本没被调用，这条代码路径从未走到。**只有真接上 LLM 才会踩到** —— 这是「骨架阶段测不出来、真机才现形」的典型坑。
**修复**：改成 `f"{k}={review.get(k)}"`，直接用 k 去 review 里取值。

> 面试答法："这个 bug 是'惰性路径'的坑——代码分支平时不走到，等真接入模型、评审真出分时才崩。我通过 review_comment 里的异常类名定位到 `_format_comment`，一眼看到循环变量 k 和 f-string 里的 v 对不上。"

### 坑 6（9/2 加入知识库后抓到）：RRF 融合把 dict 当 key，unhashable

**表象**：知识库放入文档后，`/ask` 的 `sources=[]`（检索返回空），日志 `检索失败（unhashable type: 'dict'）`。
**根因**：`rag.py` 的 `_rrf_merge` 里用 `doc`（一个 dict）直接当字典的 key 做名次累加：
```python
scores[doc] = scores.get(doc, 0.0) + 1.0 / (self.c + rank + 1)  # dict 不可哈希
```
**为什么之前没暴露**：昨天知识库是空的，`BM25.search` 直接 `return []`，根本走不到 RRF 融合这步。**一旦有真实文档被检索到，就崩** —— 又是「空数据测不出来、有数据才现形」。
**修复**：用可哈希的 `(source, content)` 元组作 key，另存一份 `best_doc` 保留原始 dict，排序后回填。

> 面试答法："RRF 要用名次融合，我一开始直接拿检索结果 dict 当 key 去累加，dict 不可哈希直接炸。改成 `(source, content)` 元组当 key。这个坑暴露得很有价值——它只在知识库非空时才出现，说明我真正把数据灌进去跑通了。"

### 坑 7（顺带）：Windows GBK 控制台写 emoji 日志崩 UnicodeEncodeError

**表象**：启动时日志刷 `UnicodeEncodeError: 'gbk' codec can't encode character '\U0001f680'`（`🚀`），污染日志。
**根因**：`setup_logging` 用 `StreamHandler(sys.stdout)`，Windows 默认 stdout 是 GBK 编码，写 emoji/非 ASCII 就崩；日志崩了会掩盖真正的业务错误。
**修复**：`setup_logging` 里 `sys.stdout/sys.stderr` 用 `reconfigure(encoding="utf-8", errors="replace")` 兜底。

### 坑 8（9/6 记忆② 验证时抓到，未修）：`hash(item.id)` 作 Qdrant point id 跨进程不稳定

**表象**：`scripts/verify_episodic.py` 连续跑两轮，同一逻辑 `item.id`（如 `v_imp_hi`）在检索结果里出现**两条重复**——写入时本该是「同 id 幂等更新」，实际变成了「两个不同 point id 的两条记录」。

**根因**：`episodic.add()` 用 Python 内建 `hash(item.id)` 作 Qdrant point id。字符串的 `hash()` 受 `PYTHONHASHSEED` 随机化，**每个进程取值不同** → 同一 `item.id` 跨进程（= 服务重启）映射到**不同的 point id**。于是「重启后重写同一条逻辑记忆」会新增一条而非覆盖，累积成重复记忆。

**为什么平时不暴露**：`memory_agent.record` 每次用 `id=f"epi_{int(time.time())}_{...}"` 生成**全新** id，天然不重复，所以正常问答链路看不到这个问题；只有「按固定 id 幂等写入」的场景（如测试、或未来想按 id 更新某条记忆）才现形。

**修复（建议 Day 13，一行）**：改用与进程无关的稳定哈希 `int.from_bytes(hashlib.md5(item.id.encode()).digest()[:8], "big", signed=True)`，保证同一 id 在任何进程映射到同一 point id，upsert 才真正幂等。

> 面试答法："我用 Python 内建 `hash()` 做持久化主键，结果发现字符串 hash 受 PYTHONHASHSEED 随机化、每个进程不一样，同一 id 跨重启会映射到不同 point id，导致重复记忆。改成 `hashlib.md5` 稳定哈希。这类'持久化 id 要进程无关'的坑，只有真把数据落盘、跨进程重跑才会暴露。"

---

## 7. 多智能体角色 + 为什么用模型B（面试 Q&A）

> 面试高频题："你这个多智能体具体有哪些 agent？""为什么评审要用另一个模型？"以下把两个问题讲透。

### Q1：你这个多智能体系统里有哪些 agent？

- 1 个主控 + 4 个干活子 Agent（+2 个辅助节点）。
- 主控（`route`）：路由，不生成内容，决定"派谁去"。代码 `graph.py` 的 `route_node`。
- 记忆子Agent（`memory_agent.py`）：`run` 检索长期记忆注入上下文；`record` 写本轮问答到情景记忆。
- 知识问答子Agent（`qa.py`）：混合检索（BM25+向量→RRF→重排）→ 基于资料+记忆生成回答。
- 工具子Agent（`tools.py`）：`run_tool` 白名单调度外部工具；`safety_gate` 安检拦截。
- 反思/评审子Agent（`reviewer.py`）：用独立模型B做四维评审，可触发修订。
- 辅助：`safety`（安检闸门）、`record`（写记忆）。

**"为什么拆成这几个？"** —— ① 职责单一，检索/生成/记忆/工具/评审各管一摊，谁坏不影响别家（降级友好）；② 能单不双，能用工具解决的不加子Agent，拆是因为"逻辑不同、必须共享状态、需要制衡"。

**一句话**："主控 + 记忆 / 问答 / 工具 / 反思四个子Agent，各自职责单一、共享同一份 AgentState，互不干扰又相互配合。"

### Q2：为什么评审要用另一个模型（模型B），不能自己评自己？

**核心**：不让模型自己评自己。

1. **同源偏差（自评 bias）**：让同一个模型A 检查自己的回答，它"编造/漏掉"的正是它自己的盲区，自己往往察觉不到（像让作者给自己当审稿人，容易自卖自夸，给出虚高分，起不到把关作用）。
2. **互补视角**：模型B 是独立模型（不同厂商/本地），训练、偏好、容错不同，A 的盲区 B 常能看出，尤其能抓**幻觉**（编造资料外内容）。
3. **真·把关 + 可控**：B 按**准确性 / 完整性 / 安全性 / 相关性**四维打分；`need_revision=true` 且给了改进版，就用改进版替换 A 的回答。
4. **降级安全**：B 失败（超时/不可用）→ 保守回退到 A 原始回答，不退化成坏结果。

**"物理隔离"怎么落地**：`config.py` 里 `MODEL_A_*` 与 `MODEL_B_*` 是**两套独立字段**，改 `.env` 就能让 B 指向完全不同的后端，代码零改动。这对应 HELLO 复盘待优化点 §14.3「双模型物理同源」——本方案从设计上就做对了。

**一句话**："QA 子Agent 生成后，交给一个物理隔离的独立模型B 做四维评审——因为自评有同源偏差、挡不住幻觉；独立评审互补视角、真把关，且 B 失败时优雅降级回退，不会产生更差的结果。"

### 附：双模型与"自反馈/反思"的关系（拓展）

- 模型B 评审 ≈ **"critic / referee"**（审稿人、反思者）：一个 Agent 生产，另一个 Agent 质检。这是 LLM 系统里经典的"反思（Reflection）"范式。
- 好处：通过"生成 + 评审"闭环提升答案质量，让系统不只是一个"会说话的模型"，而是"会自我把关的系统"。
- 面试可对比：Anthropic / OpenAI 的"Critic LLM 评 Generator"思路，说明你理解"制衡"而不只是"调用多个模型"。

---

## 8. 记忆系统：工作记忆 vs 长期记忆 + importance / source（Day 5）

> 这是本项目最大的差异化，面试必问。
> 一句话总纲：**工作记忆 = 当前会话（checkpointer 按 thread_id 持久化）；长期记忆 = episodic/semantic（跨会话）。两者是两层，别混。**

### 8.1 工作记忆 vs 长期记忆：两层，别混

**是什么**

| 维度 | 工作记忆 Working | 长期记忆 Long-term |
|---|---|---|
| 类比（MemGPT 的 OS 记忆） | RAM（当前活跃） | 磁盘缓存 + 冷存储 |
| 存什么 | 当前线程的对话历史 | episodic 情景片段 + semantic 用户事实/偏好 |
| 载体 | LangGraph `checkpointer`（`AsyncSqliteSaver` → memory.db） | episodic → Qdrant 向量；semantic → SQLite 结构化 |
| 生命周期 | 单会话，按 `thread_id` 隔离；重启不丢 | 跨会话、跨重启 |
| 检索方式 | 直接取最近 N 条 | 向量相似度 + 过期过滤 |
| 对应 `MemoryType` | `WORKING` | `EPISODIC` + `SEMANTIC`（`PROCEDURAL` 后置） |

**为什么分两层（而不是把历史全塞上下文）**

1. **上下文窗口有限、按 token 计费**：把用户全部历史塞进 prompt，token 爆炸、还稀释当前问题。工作记忆只保留「最近 N 条」，长期记忆按需检索注入——「记忆检索」vs「全量塞历史」，token 成本差数倍（Day 12 要量化这个）。
2. **会话结束不能失忆**：只靠工作记忆，客户第二次来就得从头交代。长期记忆解决「跨会话连续性」——售后场景落点就是「记住客户身份 / 历史工单 / 退换偏好」，第二次来直接说「我上次那台门锁」。
3. **业界共识（2026）**：RAG 给 context，Memory 给 continuity；vanilla RAG 不是为会话连续性设计的。67% 的 LLM 客服会话超过 20 轮，长会话是记忆刚需。

**关键细节 / 坑**

- **两层别混（面试常追问）**：`AgentState`（共享 State）是「团队/会话内记忆」——LangGraph 各子 Agent 读写同一份任务状态，解决「多智能体记忆孤岛」；`MemoryStore` 是「用户长期记忆」——跨会话的用户画像。一个是 graph 内部协调，一个是跨会话持久化。
- **工作记忆要截断**：`extract_recent_messages` 保头 1（system 说明不丢）+ 尾 N-1，防上下文膨胀。
- **长期记忆要 curation**：episodic 带 TTL 遗忘，semantic 要固化（去重/矛盾合并），否则变「垃圾抽屉」（业界原话：semantic memory 不做 curation 就是 junk drawer）。
- ⚠️ `extract_recent_messages` 的 `messages[-(keep-1):]` 在 `keep=1` 时 `-0 == 0` 退化成全量（Python 陷阱，Day 5 发现未修，记在 `项目日志-2026-09-05.md`）。

**面试怎么答**

- **Q：工作记忆和长期记忆有什么区别？**
  A：工作记忆 = 当前会话，靠 checkpointer 按 thread_id 持久化，重启不丢，但只管「这次对话」；长期记忆 = episodic（某次对话发生了什么，向量化 + TTL）+ semantic（用户是谁/偏好，结构化 + 去重），跨会话按相似度检索。一个解决「这次聊什么」，一个解决「下次还认不认识你」。

- **Q：为什么不把历史对话全塞进上下文？**
  A：token 成本和上下文窗口都有限，全塞会稀释当前问题。我分两层：工作记忆只保留最近 N 条，长期记忆按需检索注入——这就是「记忆检索」和「全量塞历史」的核心区别。

### 8.2 importance：记忆权重 / 遗忘优先级

**是什么**：`MemoryItem.importance`，0~1 浮点，默认 0.5，标记「这条记忆有多值得记」。

**三个作用**

1. **检索排序**：命中多条记忆时，importance 高者靠前（与相似度共同决定注入顺序）。
2. **遗忘优先级**：episodic 带 TTL（`MEMORY_TTL_DAYS`）淘汰时，importance 低者先过期——「不重要的事先忘」。
3. **固化保留**：consolidator 提炼时，importance 高的优先保留，避免「重要事实被摘要掉」。

**面试怎么答**

- **Q：importance 是干嘛的？**
  A：记忆的权重，0~1。决定三件事：检索时排前、TTL 淘汰时后忘、固化时优先保留。比如「用户是金卡会员」importance 就该比「寒暄了一句」高。它把「记忆」从「都存」升级成「按重要性管理」。

### 8.3 source：来源溯源

**是什么**：`MemoryItem.source` 记录这条记忆是谁产生的：`"user"`（用户亲口说）/ `"qa"`（问答子Agent 提取）/ `"tool"`（工具产生）/ `"consolidator"`（固化子Agent 提炼）。

**作用**

1. **溯源可审计**：每条记忆能查到「是用户说的还是模型推断的」——企业场景合规刚需。
2. **可信度区分**：`user` 来源（用户明说的偏好）可信度 > `consolidator`（模型摘要推断），检索/固化时可按来源加权。
3. **排错定位**：记忆错了能定位「是哪个 Agent 生成的」。

**面试怎么答**

- **Q：source 字段有什么用？**
  A：来源溯源。区分「用户说的」和「模型推断的」——用户亲口说的偏好可信度高，固化子Agent 摘要推断的次之。既方便审计，也能在记忆出问题时定位是哪个环节产生的。

### 8.4 一分钟串讲（Day 5 背这个）

> 「我的记忆系统分两层：**工作记忆** = 当前会话，靠 LangGraph checkpointer 按 thread_id 持久化，重启不丢；**长期记忆** = episodic（情景片段，向量化 + TTL 遗忘）+ semantic（用户事实/偏好，结构化 + 去重合并），跨会话按相似度检索注入。每条记忆带 `importance`（权重/遗忘优先级）和 `source`（来源溯源）两个字段。关键点是这两层别混：共享 State 是团队会话内记忆，MemoryStore 是用户长期记忆。」
